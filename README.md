# MacOSX-screensavers
screensavers for macos

NeonClock in ObjC for macos makes a NeonClock.saver file that you double click on to import into MacOSX

log stream --info --style compact --predicate 'eventMessage CONTAINS "NeonClock"'